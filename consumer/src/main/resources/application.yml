server:
  port: 8080
spring:
  application:
    name: consumer
  thymeleaf:
    cache: false # 开发时关闭缓存
  # 禁用 Redis/Kafka 自动配置，改由 RedisConfig 按开关手动创建，避免无中间件时启动卡顿
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:localhost:8848}  # 通过环境变量注入
        namespace: ${NACOS_NAMESPACE:public}
        username: ${NACOS_USERNAME:nacos}
        password: ${NACOS_PASSWORD:nacos}

# 中间件开关与连接配置（默认关闭，ENABLE_XXX=true 时启用对应中间件）
app:
  middleware:
    redis:
      enabled: ${ENABLE_REDIS:false}
      # Redis 部署模式：不填=单机(host:port) | sentinel=哨兵 | cluster=集群
      mode: ${REDIS_MODE:}
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      database: ${REDIS_DATABASE:0}
      # 哨兵模式（REDIS_MODE=sentinel 时生效）
      sentinel:
        master: ${REDIS_SENTINEL_MASTER:mymaster}
        nodes: ${REDIS_SENTINEL_NODES:}     # 逗号分隔，如 sentry1:26379,sentry2:26379
      # 集群模式（REDIS_MODE=cluster 时生效）
      cluster:
        nodes: ${REDIS_CLUSTER_NODES:}      # 逗号分隔，如 node1:6379,node2:6379,node3:6379
    kafka:
      enabled: ${ENABLE_KAFKA:false}
      bootstrapServers: ${KAFKA_SERVER:localhost:9092}
      topic: consumer-access-event
